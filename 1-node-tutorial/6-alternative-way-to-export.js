// array
// we can do this instead of doing const items = ['item1', 'item2']
module.exports.items = ['item1', 'item2']

// object
const person = {
   name: 'bob',
}

// we can export like this for all data types
module.exports.singlePerson = person